import 'package:flutter/material.dart';

class SaleMenuWidget extends StatelessWidget {
  const SaleMenuWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}